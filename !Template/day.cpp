#include <bits/stdc++.h>
using namespace std;

using ll = long long;
#define all(v) begin(v), end(v)

void calculate1() {
    int ans = 0;
    string s;
    while(cin >> s) {
       cout << s << "\n";
    }
    
    cout << ans << "\n";
}


void calculate2() {
    // ========!!remember to comment out the other calculate function!!========
    int ans = 0;
    string s;
    while(cin >> s) {
       cout << s << "\n";
    }
    
    cout << ans << "\n";
}

int main() {
    cin.tie(0)->ios::sync_with_stdio(0);


    // ========!!remember to comment out the other calculate function!!========

    freopen("input.in", "r", stdin);

    cout << "P1: "; calculate1();

    cout << "P2: "; calculate2();

}
